// Презентация по проекту «Мобильное приложение для трейдинга и инвестиций».
// Самодостаточный шаблон 16:9 без внешних пакетов (компилируется офлайн):
//   typst compile slides.typ slides.pdf
// Структура повторяет отчёт: введение → ТЗ → архитектура → реализация →
// тестирование → заключение. Рассчитана на ~7 минут (16 слайдов).

#set document(title: "Мобильное приложение для трейдинга и инвестиций")

#let accent = rgb("#0b7285")
#let accent2 = rgb("#1565c0")
#let muted = rgb("#566270")
#let soft = rgb("#eef3f4")

#set page(
  paper: "presentation-16-9",
  margin: (top: 1.6cm, bottom: 1.2cm, left: 1.7cm, right: 1.7cm),
  fill: white,
  footer: context {
    set text(10pt, fill: muted)
    grid(
      columns: (1fr, auto),
      align: (left, right),
      [Мобильный биржевой терминал · ИТМО, 2026],
      [#counter(page).display()],
    )
  },
)

#set text(font: ("Liberation Sans", "DejaVu Sans"), size: 21pt, lang: "ru")
#set par(justify: false, leading: 0.62em)
#show strong: set text(fill: accent2)
#set list(marker: (text(fill: accent)[▪], text(fill: accent)[–]), spacing: 0.9em)

// Обычный контентный слайд: заголовок + линия + тело.
#let slide(title, body) = {
  pagebreak(weak: true)
  text(size: 29pt, weight: "bold", fill: accent)[#title]
  v(-3pt)
  line(length: 100%, stroke: 2pt + accent)
  v(10pt)
  set text(size: 20pt)
  body
}

// ─────────────────────────── Титульный слайд ───────────────────────────
#page(footer: none)[
  #set align(center + horizon)
  #text(size: 16pt, fill: muted)[
    Национальный исследовательский университет ИТМО \
    Факультет программной инженерии и компьютерной техники \
    Дисциплина «Разработка мобильных приложений»
  ]
  #v(1.0em)
  #block(width: 90%)[
    #text(size: 38pt, weight: "bold", fill: accent)[
      Мобильное приложение для трейдинга и инвестиций
    ]
  ]
  #v(0.3em)
  #text(size: 20pt, fill: muted)[
    Полная экосистема биржевого терминала: \
    от драйвера ядра Linux до Android-клиента на 10 000 клиентов
  ]
  #v(1.6em)
  #text(size: 17pt)[
    Покалюхин И. И. · Нестеров В. А. · Джохадзе А. Б. · \
    Бондаренко А. А. · Лашкул А. В.
  ]
  #v(0.5em)
  #text(size: 15pt, fill: muted)[Санкт-Петербург · 2026]
]

// ─────────────────────────── Введение ───────────────────────────
#slide[Актуальность][
  - Более *30 млн* частных инвесторов на Московской бирже; *> 70 %* сделок
    на рынке акций — физические лица.
  - Основной канал доступа — *мобильные приложения*: смартфон фактически
    заменил биржевой терминал.
  - Жёсткие требования: котировки *в реальном времени*, *корректные и
    атомарные* сделки, *тысячи одновременных* клиентов в пик волатильности.
  - Задача охватывает *весь стек*: от ядра ОС до мобильного клиента и
    наблюдаемой распределённой серверной части.
]

#slide[Цель и задачи][
  *Цель* — спроектировать и реализовать экосистему биржевого терминала,
  обслуживающую *10 000 одновременных клиентов*.

  #v(6pt)
  *Задачи:*
  - драйвер-источник котировок в ядре Linux;
  - сбор котировок: брокер сообщений + история;
  - сервис БД: счета, ордера, сделки, матчинг лимиток;
  - gateway: аутентификация, WebSocket, проксирование;
  - Android-приложение (Jetpack Compose);
  - нагрузочное тестирование на 10 000 клиентов и наблюдаемость.
]

// ─────────────────────────── Техническое задание ───────────────────────────
#slide[ТЗ: назначение и заинтересованные лица][
  *Назначение.* Доступ частного инвестора к биржевым торгам со смартфона:
  котировки, история, заявки, портфель, история операций.

  #v(8pt)
  *Заинтересованные лица* (основа для требований):
  #v(4pt)
  #grid(
    columns: (1fr, 1fr),
    gutter: 10pt,
    [- частный инвестор (пользователь)
     - брокерская компания (заказчик)
     - поставщик котировок (биржа)
     - разработчики mobile / backend],
    [- инженер эксплуатации (DevOps/SRE)
     - ИБ и комплаенс
     - тестировщик (QA)
     - служба поддержки],
  )
]

#slide[ТЗ: функции и состав системы][
  #grid(
    columns: (1fr, 1fr),
    gutter: 16pt,
    [
      *Функции*
      - регистрация и аутентификация
      - котировки в реальном времени
      - история-свечи (1m…1d)
      - заявки Market / Limit
      - матчинг лимиток
      - портфель и P\&L, история сделок
    ],
    [
      *Состав (7 компонентов + инфраструктура)*
      - \#7 драйвер · \#6 quotes
      - \#4 data-сервис · \#3 gateway
      - \#1 Android · \#2 RN (опц.)
      - \#5 нагрузочный имитатор
      - Redis · PostgreSQL · ClickHouse
      - OpenTelemetry / Jaeger · Docker
    ],
  )
]

#slide[ТЗ: характеристики системы][
  - *Производительность:* до 10 000 клиентов, целевой p99 ≤ 250 мс.
  - *Надёжность:* доля ошибок < 0,1 %; развязка через брокер; атомарные
    торговые транзакции.
  - *Безопасность:* JWT, bcrypt, только параметризованный SQL, rate limit.
  - *Энергопотребление (мобильный аспект):* push по одному WebSocket вместо
    опроса; перерисовка только изменившихся элементов.
  - *Инструментальный аспект:* единый язык Kotlin, Docker, OpenTelemetry.
]

// ─────────────────────────── Архитектура ───────────────────────────
#slide[Архитектура системы][
  #align(center)[
    #block(fill: soft, inset: 12pt, radius: 6pt)[
      #set text(font: ("DejaVu Sans Mono", "Liberation Mono"), size: 14pt)
      #set align(left)
      ```
      Android (#1)            Load Simulator (#5)
            └───── REST + WebSocket ─────┘
                         │
                 Gateway #3 (Ktor)  ·  JWT · rate limit · WS
                   │             │
          Data-сервис #4       Redis ◄── Quotes #6 (Go) ◄── Драйвер C #7
           │         │        Pub/Sub        │               /dev/stock0..3
      PostgreSQL   Redis      кэш цен        ▼
                                         ClickHouse
      ```
    ]
  ]
  #v(6pt)
  #text(size: 18pt)[
    *Котировки:* драйвер → quotes → Redis / ClickHouse → Gateway → клиенты. \
    *Торговля:* клиент → Gateway (JWT) → Data-сервис — сделка в одной транзакции.
  ]
]

#slide[Архитектура: технологии и данные][
  - *Языки и платформы:* C — драйвер; Go — quotes и нагрузочный имитатор;
    Kotlin + Ktor — backend; Jetpack Compose — Android.
  - *Хранилища:* Redis — шина тиков и кэш цен; PostgreSQL — счета и сделки
    (транзакции); ClickHouse — история тиков и свечи.
  - *База данных:* `users`, `accounts`, `instruments`, `orders`, `trades`,
    `positions`; в ClickHouse — `ticks` и материализованные `candles_1m`.
  - *Принцип:* слабая связанность, единая точка входа (gateway), голый SQL
    без ORM.
]

// ─────────────────────────── Реализация ───────────────────────────
#slide[Реализация: источник котировок (\#7, \#6)][
  - *Драйвер ядра Linux:* символьные устройства `/dev/stock0..3`, цена по
    модели геометрического броуновского движения, запись 32 байта, настройка
    через `ioctl`.
  - *Сервис quotes (Go):* читает тики → публикует в Redis Pub/Sub + батч-запись
    истории в ClickHouse; метрики в формате Prometheus.
  - *Отказоустойчивость:* встроенный GBM-симулятор, когда устройства
    недоступны (например, не-Linux окружение).
]

#slide[Реализация: бэкенд и торговля (\#4, \#3)][
  - *Data-сервис:* голый SQL + HikariCP. Ордер исполняется атомарно:
    `SELECT … FOR UPDATE` → проверка средств → запись сделки → пересчёт
    позиции и баланса.
  - *Лимитные заявки:* фоновый воркер по потоку тиков
    (`FOR UPDATE SKIP LOCKED`); идемпотентность по `clientOrderId`.
  - *Gateway:* bcrypt + JWT, WebSocket fan-out (одна подписка на Redis на
    процесс), rate limiting, проксирование с подстановкой `userId` из токена.
]

#slide[Реализация: Android-приложение][
  #grid(
    columns: (1.05fr, auto),
    gutter: 18pt,
    align: (left + horizon, center),
    [
      - Kotlin + Jetpack Compose, *MVVM* (ViewModel + StateFlow).
      - Экраны: рынок (живые цены по WebSocket), карточка инструмента с
        графиком и формой ордера, портфель с P\&L, история заявок и сделок.
      - JWT в DataStore; *авто-refresh* токена при 401 и повтор запроса.
    ],
    grid(
      columns: 2,
      gutter: 8pt,
      image("img/market.png", height: 10.4cm),
      image("img/instrument.png", height: 10.4cm),
    ),
  )
]

#slide[Реализация: ключевые технические решения][
  - *Redis Pub/Sub как шина тиков* — развязка генерации котировок и
    потребителей; отказ одного потребителя не влияет на других.
  - *Денежные поля строками в JSON* — сохранение точности `NUMERIC`.
  - *Сквозной трейсинг* — W3C `traceparent` (OpenTelemetry), спаны SQL —
    дети клиентского запроса.
  - *Защита от перегрузки* — rate limiting + неблокирующие буферы тиков.
]

// ─────────────────────────── Тестирование ───────────────────────────
#slide[Тестирование: корректность][
  - *Юнит-тесты* бизнес-логики: маппинг символов, средняя цена позиции,
    пересечение лимиток, JWT, перцентили.
  - *Скриншот-тесты UI* без эмулятора (Robolectric Native Graphics +
    Roborazzi) — экраны рендерятся в CI.
  - *Интеграционный сценарий* на реальном окружении: регистрация → market-ордер
    → сделка → портфель; лимитка исполняется воркером; идемпотентность повторного
    запроса.
]

#slide[Тестирование: нагрузка 10 000 клиентов][
  #grid(
    columns: (auto, 1fr),
    gutter: 20pt,
    align: (left + horizon, left + horizon),
    table(
      columns: (auto, auto, auto, auto),
      inset: 7pt,
      align: (left, right, right, right),
      stroke: 0.5pt + muted,
      table.header([*Действие*], [*Запросов*], [*p99, мс*], [*Ошибок*]),
      [quotes], [110 119], [120], [0],
      [portfolio], [49 257], [123], [0],
      [order], [36 987], [144], [5],
      [trades], [24 543], [120], [0],
      [history], [24 277], [209], [5],
      [register], [10 000], [122], [0],
    ),
    [
      - Всего *255 183* запроса, *10* ошибок (*0,004 %*).
      - p99 ≤ *210 мс* по всем операциям.
      - WebSocket: *1 000* соединений, *6,08 млн* тиков, *0* переподключений.
      - Цель «10 000 клиентов» *подтверждена*.
    ],
  )
]

// ─────────────────────────── Заключение ───────────────────────────
#slide[Заключение][
  - Реализована *полная экосистема* биржевого терминала: драйвер → quotes →
    data-сервис → gateway → Android-приложение.
  - Цель достигнута: *10 000 клиентов*, p99 ≤ 210 мс, доля ошибок 0,004 %.
  - Обеспечена *наблюдаемость*: сквозные трейсы, метрики, структурированные логи.
  - *Направления развития:* частичное исполнение ордеров, стакан заявок и
    матчинг «клиент против клиента», горизонтальное масштабирование Gateway.
]

#page(footer: none)[
  #set align(center + horizon)
  #text(size: 40pt, weight: "bold", fill: accent)[Спасибо за внимание!]
  #v(0.6em)
  #text(size: 24pt, fill: muted)[Вопросы?]
  #v(1.4em)
  #text(size: 18pt)[Мобильное приложение для трейдинга и инвестиций · ИТМО, 2026]
]
