# Jetpack Compose, MVVM и StateFlow

Android-приложение (`#1`) написано на **Jetpack Compose** с архитектурой
**MVVM** и потоками состояния **StateFlow**. Это современный стандарт Android, и
он устроен иначе, чем «старый» Android с XML-разметкой. Разберём по слоям.

## Jetpack Compose: декларативный UI

Раньше UI на Android описывали XML-разметкой, находили элементы по id и **вручную
меняли** их из кода (`textView.text = "..."`). Это **императивно**: ты сам
дёргаешь каждое изменение, и легко рассинхронить экран с данными.

Compose — **декларативный**: ты описываешь, *как выглядит экран при данном
состоянии*, функцией. Меняется состояние → Compose сам перерисовывает что нужно.

```kotlin
@Composable
fun MarketScreen(state: MarketState) {
    LazyColumn {
        items(state.quotes) { q ->
            Text("${q.symbol}  ${q.last}")   // меняется q.last → строка сама обновится
        }
    }
}
```

Функции с `@Composable` — это «кирпичики» UI. Когда данные, которые они читают,
меняются, Compose делает **рекомпозицию** — перевызывает только затронутые
кусочки и перерисовывает минимально нужное. Никаких ручных `findViewById` и
обновлений «руками».

## MVVM: кто за что отвечает

**MVVM** (Model–View–ViewModel) разделяет экран на слои:

- **Model** — данные и бизнес-логика (репозитории, сетевой клиент
  [[DataStore и авто-refresh токена|ApiClient]], модели).
- **View** — `@Composable`-функции. Только рисуют состояние и шлют события
  («нажали Купить»). Никакой логики.
- **ViewModel** — посредник: держит состояние экрана, обрабатывает события,
  ходит в Model, обновляет состояние. Переживает поворот экрана (не пересоздаётся
  вместе с Activity).

```
View (Compose) ──события──▶ ViewModel ──▶ Model (Repository/Api)
      ▲                          │
      └────── состояние ─────────┘
```

> [!tip] Зачем разделять
> View не знает, *откуда* данные, Model не знает про UI. Логику можно
> [[Скриншот-тестирование UI|тестировать]] без экрана, а экран — рисовать с
> «фейковым» состоянием. Меньше связности — меньше боли.

## StateFlow: состояние как поток

Как ViewModel отдаёт состояние во View, чтобы та сама перерисовывалась?
Через **StateFlow** — «наблюдаемый контейнер с текущим значением»:

```kotlin
class MarketViewModel(...) : ViewModel() {
    private val _state = MutableStateFlow(MarketState())
    val state: StateFlow<MarketState> = _state      // наружу — только для чтения

    fun onTick(tick: Tick) {
        _state.update { it.copy(quotes = it.quotes.updatePrice(tick)) }  // новое состояние
    }
}
```

А Compose подписывается на него и автоматически рекомпозит UI при изменении:

```kotlin
val state by viewModel.state.collectAsStateWithLifecycle()
MarketScreen(state)
```

Свойства StateFlow, которые тут важны:

- всегда есть **текущее значение** (новый подписчик сразу его получает);
- отдаёт **только при изменении** — если новое значение равно старому, лишней
  рекомпозиции не будет;
- это **однонаправленный поток данных** (UDF): состояние течёт вниз (VM → View),
  события — вверх (View → VM). Одно «единственно верное» состояние, его не
  правят из десяти мест.

> [!note] Откуда живые цены
> Котировки прилетают по [[WebSocket и fan-out котировок|WebSocket]] →
> ViewModel кладёт их в `MutableStateFlow` → Compose сам перерисовывает строки.
> График инструмента рисуется на `Canvas` вручную из точек цены.

DI (внедрение зависимостей) здесь **ручной** — простой `AppContainer` создаёт и
раздаёт зависимости, без Dagger/Hilt. Для приложения такого размера этого
достаточно и понятнее.

## Связанное

- [[DataStore и авто-refresh токена]] — как клиент ходит в сеть и хранит JWT.
- [[Ktor и корутины Kotlin]] — тот же Kotlin и корутины, что на бэкенде.
- [[Скриншот-тестирование UI]] — как проверяем эти экраны без эмулятора.
