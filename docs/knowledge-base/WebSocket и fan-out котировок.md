# WebSocket и fan-out котировок

Котировки должны лететь на экран **в реальном времени** — цена дёрнулась, цифра
тут же обновилась. Для этого Gateway (`#3`) раздаёт тики клиентам по **WebSocket**,
а внутри применяет приём под названием **fan-out**. Разберём оба.

## Почему WebSocket, а не обычный HTTP

Обычный HTTP — это «вопрос-ответ»: клиент спросил, сервер ответил, соединение
закрылось. Чтобы получать свежие цены так, пришлось бы **опрашивать** сервер
(polling) каждую секунду — куча лишних запросов, задержка, разряд батареи на
телефоне.

Варианты доставки «сервер → клиент»:

| Способ | Как работает | Минус |
|---|---|---|
| Polling | клиент дёргает `/quotes` по таймеру | задержка, трафик, батарея |
| SSE | один незакрытый HTTP, сервер шлёт строки | только в одну сторону |
| **WebSocket** | постоянное двустороннее соединение | чуть сложнее |

**WebSocket** — это апгрейд HTTP-соединения до постоянного двустороннего канала.
Открыли один раз — и дальше сервер **сам пушит** тики, как только они появились,
а клиент по тому же каналу шлёт команды (подписаться на `SBER`, отписаться).
Идеально для котировок и щадит батарею телефона (одно соединение вместо
бесконечного опроса).

```
client → { "action":"subscribe", "symbols":["SBER","BTC"] }
server → { "symbol":"SBER","price":305.5,"ts":… }   ← и так на каждый тик
```

## Fan-out: один источник → тысячи получателей

Теперь главная хитрость. Представь 1000 клиентов с открытым WebSocket. Тики
лежат в [[Redis — Pub-Sub и кэш|Redis-канале]] `quotes:ticks`. Наивное решение —
**на каждого клиента своя подписка на Redis**. 1000 клиентов = 1000 подписок к
Redis. Это лишняя нагрузка и на Redis, и на сеть.

Решение — **fan-out в памяти**: Gateway держит **ровно одну** подписку на Redis
на весь процесс, а каждый прилетевший тик «разветвляет» всем
WebSocket-клиентам внутри себя. В Kotlin для этого идеально подходит
`SharedFlow` — «горячий» поток, на который могут подписаться много слушателей:

```kotlin
// одна подписка на Redis на весь Gateway
private val tickFlow = MutableSharedFlow<Tick>(
    extraBufferCapacity = 8192,
    onBufferOverflow = BufferOverflow.DROP_OLDEST,   // ← см. ниже
)

pubSub.subscribe("quotes:ticks")        // ← ОДИН раз
override fun message(channel, message) {
    tickFlow.tryEmit(tick)              // раздать всем подписчикам Flow
}
```

Каждое WebSocket-соединение просто `collect`-ит этот `SharedFlow` и
пере-отправляет клиенту те символы, на которые он подписался.

```
Redis quotes:ticks ──одна подписка──▶ SharedFlow ──┬──▶ клиент 1 (SBER)
                                                    ├──▶ клиент 2 (BTC)
                                                    └──▶ клиент 3 (SBER, GAZP)
```

## `DROP_OLDEST`: медленный клиент не тормозит остальных

Что если один клиент на медленном мобильном интернете не успевает забирать тики?
Его буфер переполнится. Тут снова всплывает сквозной принцип системы (тот же, что
в [[Модуль ядра Linux и символьные устройства|драйвере]] и
[[Go — горутины, каналы и backpressure|quotes-go]]):

> [!tip] Лучше потерять старый тик, чем затормозить всех
> `onBufferOverflow = BufferOverflow.DROP_OLDEST` — при переполнении буфера
> выкидывается самый старый тик. Тормозной клиент теряет пару устаревших цен (они
> уже неактуальны!), но **не блокирует** раздачу остальным. Для котировок свежесть
> важнее полноты.

> [!note] Кэш последнего тика
> Рядом Gateway держит `ConcurrentHashMap` с последним тиком каждого символа —
> чтобы мгновенно ответить на REST-запрос `/quotes` и отдать стартовую цену
> новому подписчику, не дожидаясь следующего тика. Подробнее —
> [[Redis — Pub-Sub и кэш]].

## Связанное

- [[Redis — Pub-Sub и кэш]] — источник тиков.
- [[Ktor и корутины Kotlin]] — WebSockets-плагин и `SharedFlow`.
- [[Нагрузочное тестирование и перцентили]] — там 1000 WS-клиентов держат поток.
